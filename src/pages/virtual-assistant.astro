---
import BaseLayout from "../layouts/BaseLayout.astro";

const serviceSchema = {
  "name": "Executive Virtual Assistant & Remote Operations Support Services",
  "serviceType": [
    "executive virtual assistant services",
    "remote executive assistant",
    "virtual assistant for founders",
    "virtual assistant for CEOs",
    "hire a virtual assistant",
    "online virtual assistant",
    "administrative support services",
    "inbox management services",
    "calendar management services",
    "project management virtual assistant",
    "remote admin support",
    "operations support services",
    "delegate business tasks",
    "virtual assistant for entrepreneurs",
    "AI executive assistant support",
    "human virtual assistant paired with AI",
  ],
  "description": "Executive virtual assistant services for founders, CEOs, and growing teams: remote admin support, inbox management, calendar control, project management, and operational follow-through — without the overhead of a full-time hire.",
};

const vaMarquee = [
  "Inbox Zero", "Calendar Cleared", "Follow-ups Sent", "Reports Filed",
  "Projects Tracked", "Meetings Scheduled", "Tasks Delegated", "Operations Handled",
  "Brief Prepared", "Deadlines Hit", "Stakeholders Aligned", "No Detail Missed",
];
---

<BaseLayout
  title="Executive Virtual Assistant Services for Founders & CEOs | LuliDigital"
  description="Executive virtual assistant services for founders, CEOs, and growing teams: remote admin support, inbox management, calendar control, project management, and operational follow-through without a full-time hire."
  active="virtual-assistant"
  serviceSchema={serviceSchema}
>

  <!-- ═══════════════════════════════════════════════════════ HERO -->
  <section class="va-hero" aria-label="Virtual Assistant services" data-bee-stop>
    <div class="va-desk-field" aria-hidden="true"></div>

    <div class="va-hero-split">
      <div class="va-hero-text">
        <span class="eyebrow va-eyebrow" data-blur-in>Executive Ops Desk</span>
        <h1 class="va-title" data-word-reveal>
          Executive support without the full-time overhead.
        </h1>
        <p class="va-sub" data-blur-in data-blur-delay="200">
          Dedicated remote support for founders and CEOs — inbox, calendar, operations, and projects handled so you can lead.
        </p>
        <div class="va-ctas" data-stagger data-stagger-gap="80">
          <a class="pill pill--filled magnetic" href="https://wa.me/27602551513">Hire your VA →</a>
          <a class="pill pill--outline magnetic" href="/ai">AI Desk →</a>
        </div>
      </div>

      <!-- Animated task board character -->
      <div class="va-board" aria-hidden="true">
        <div class="vb-header">
          <span class="vb-dot vb-dot--r"></span>
          <span class="vb-dot vb-dot--y"></span>
          <span class="vb-dot vb-dot--g"></span>
          <span class="vb-title-bar">Today's Desk</span>
        </div>
        <div class="vb-tasks">
          <div class="vb-task vb-task--done" style="--ti:0">
            <span class="vb-check">✓</span>
            <span>Inbox cleared — 47 emails</span>
          </div>
          <div class="vb-task vb-task--done" style="--ti:1">
            <span class="vb-check">✓</span>
            <span>3 exec calls scheduled</span>
          </div>
          <div class="vb-task vb-task--done" style="--ti:2">
            <span class="vb-check">✓</span>
            <span>Q3 brief sent to team</span>
          </div>
          <div class="vb-task vb-task--active" style="--ti:3">
            <span class="vb-spinner"></span>
            <span>Weekly report — in progress</span>
          </div>
          <div class="vb-task vb-task--pending" style="--ti:4">
            <span class="vb-ring"></span>
            <span>Review invoice batch</span>
          </div>
        </div>
        <div class="vb-footer">
          <span class="vb-status-dot"></span>
          <span class="vb-status-text">VA active now</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════ MARQUEE -->
  <div class="marquee-wrap" aria-hidden="true">
    <div class="marquee-track">
      {[...vaMarquee, ...vaMarquee].map((item) => (
        <span class="marquee-item">{item}</span>
      ))}
    </div>
  </div>

  <section class="selling-point selling-point--va" aria-label="Human virtual assistant paired with AI selling point" data-reveal="up" data-bee-stop>
    <span class="eyebrow">Human + AI Support</span>
    <h2 class="selling-title">You get a human virtual assistant paired with an AI executive assistant.</h2>
    <p class="selling-copy">
      During the day, a real person keeps your inbox, calendar, projects, follow-ups, and operations moving with care.
      When the human sleeps, AI support can still answer, organise, route, and prepare the next step so your business
      does not go quiet just because the day ended.
    </p>
    <p class="selling-copy selling-copy--direct">
      Want to know how this would support your week? Talk to the desk and we will show you what can be delegated first.
    </p>
    <div class="selling-links">
      <a class="pill pill--filled magnetic" href="https://wa.me/27602551513">WhatsApp the desk</a>
      <a class="pill pill--outline magnetic" href="mailto:info@lulidigital.co.za">Email the desk</a>
    </div>
  </section>

  <!-- ════════════════════════════════════════ DELEGATION SVG -->
  <section class="delegation-section" aria-label="How delegation works" data-reveal="up" data-bee-stop>
    <span class="eyebrow">How Delegation Works</span>
    <h2 class="section-title">If it takes your time, it can be delegated.</h2>

    <div class="delegation-wrap">
      <svg class="delegation-svg" viewBox="0 0 660 180" aria-label="Delegation flow: You hand tasks to the Virtual Assistant who handles them, resulting in Done">
        <defs>
          <marker id="arr-del" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
            <path d="M0,0 L0,6 L8,3 z" fill="rgba(123,45,45,.55)"/>
          </marker>
        </defs>
        <rect x="10" y="40" width="140" height="100" rx="16" fill="var(--paper-card,#f6f2ea)" stroke="rgba(123,45,45,.28)" stroke-width="1.5"/>
        <text x="80" y="72"  text-anchor="middle" class="del-big">You</text>
        <text x="80" y="92"  text-anchor="middle" class="del-small">Founder / CEO</text>
        <text x="80" y="110" text-anchor="middle" class="del-small">Decision maker</text>
        <path d="M154 90 L210 90" stroke="rgba(123,45,45,.45)" stroke-width="1.5" marker-end="url(#arr-del)"/>
        <rect x="215" y="40" width="230" height="100" rx="16" fill="rgba(123,45,45,.88)" stroke="none"/>
        <text x="330" y="72"  text-anchor="middle" class="del-big-inv">Virtual Assistant</text>
        <text x="330" y="92"  text-anchor="middle" class="del-small-inv">Inbox · Calendar · Tasks</text>
        <text x="330" y="110" text-anchor="middle" class="del-small-inv">Projects · Follow-ups</text>
        <path d="M450 90 L506 90" stroke="rgba(123,45,45,.45)" stroke-width="1.5" marker-end="url(#arr-del)"/>
        <rect x="511" y="40" width="140" height="100" rx="16" fill="var(--paper-card,#f6f2ea)" stroke="rgba(123,45,45,.28)" stroke-width="1.5"/>
        <text x="581" y="82"  text-anchor="middle" class="del-big">Done.</text>
        <text x="581" y="102" text-anchor="middle" class="del-small">You focus on what</text>
        <text x="581" y="118" text-anchor="middle" class="del-small">only you can do.</text>
      </svg>
      <pre class="delegation-ascii ascii-diagram" aria-hidden="true">
<span class="hi">You</span> ──────▶ <span class="hi">Virtual Assistant</span> ──────▶ <span class="hi">Done.</span>
Founder       Inbox · Calendar · Tasks     You focus on
/ CEO         Projects · Follow-ups        what matters.</pre>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════ METRICS STRIP -->
  <div class="metrics-strip" aria-label="VA impact" data-reveal="up">
    <div class="metric-item">
      <span class="metric-num" data-count="40" data-count-suffix="h+">0</span>
      <span class="metric-label">freed per week</span>
    </div>
    <div class="metric-divider" aria-hidden="true"></div>
    <div class="metric-item">
      <span class="metric-num" data-count="200" data-count-suffix="+">0</span>
      <span class="metric-label">tasks handled monthly</span>
    </div>
    <div class="metric-divider" aria-hidden="true"></div>
    <div class="metric-item">
      <span class="metric-num" data-count="3" data-count-suffix="×">0</span>
      <span class="metric-label">more leadership time</span>
    </div>
  </div>

  <!-- ══════════════════════════════════════════ SERVICE BENTO GRID -->
  <section class="va-services" aria-label="VA services" data-reveal="up" data-bee-stop>
    <span class="eyebrow">What We Handle</span>
    <div class="bento-grid" data-stagger data-stagger-gap="80">

      <article class="glow-card svc-card" data-tilt="7" data-reveal="float-l" data-reveal-delay="0">
        <div class="tilt-layer" style="display:flex;flex-direction:column;gap:12px;height:100%">
          <div class="svc-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
          </div>
          <h3 class="svc-title">Admin &amp; Operations</h3>
          <ul class="bento-list">
            <li style="--li-i:0">Inbox managed &amp; organised</li>
            <li style="--li-i:1">Calendar controlled &amp; optimised</li>
            <li style="--li-i:2">Follow-ups sent on schedule</li>
            <li style="--li-i:3">Reports prepared &amp; distributed</li>
          </ul>
          <p class="svc-footer">Quiet execution behind the scenes.</p>
        </div>
      </article>

      <article class="glow-card svc-card" data-tilt="7" data-reveal="drop" data-reveal-delay="100">
        <div class="tilt-layer" style="display:flex;flex-direction:column;gap:12px;height:100%">
          <div class="svc-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-3 3"/></svg>
          </div>
          <h3 class="svc-title">Project Management</h3>
          <ul class="bento-list">
            <li style="--li-i:0">Timelines owned &amp; maintained</li>
            <li style="--li-i:1">Milestones tracked and hit</li>
            <li style="--li-i:2">Teams aligned without chasing</li>
            <li style="--li-i:3">Deliverables managed end-to-end</li>
          </ul>
          <p class="svc-footer">Nothing slips. Nothing gets lost.</p>
        </div>
      </article>

      <article class="glow-card svc-card" data-tilt="7" data-reveal="float-r" data-reveal-delay="200">
        <div class="tilt-layer" style="display:flex;flex-direction:column;gap:12px;height:100%">
          <div class="svc-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="m3 11 19-9-9 19-2-8-8-2z"/></svg>
          </div>
          <h3 class="svc-title">Marketing &amp; Content Support</h3>
          <ul class="bento-list">
            <li style="--li-i:0">Content created and scheduled</li>
            <li style="--li-i:1">Campaigns structured and briefed</li>
            <li style="--li-i:2">Social media managed</li>
            <li style="--li-i:3">Brand message distributed</li>
          </ul>
          <p class="svc-footer">Your message out. Consistently.</p>
        </div>
      </article>

      <article class="glow-card svc-card" data-tilt="7" data-reveal="pop" data-reveal-delay="300">
        <div class="tilt-layer" style="display:flex;flex-direction:column;gap:12px;height:100%">
          <div class="svc-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="8" width="18" height="12" rx="2"/><path d="M7 8V6a2 2 0 0 1 2-2h6a2 2 0 0 1 2 2v2"/><circle cx="12" cy="14" r="2"/></svg>
          </div>
          <h3 class="svc-title">AI &amp; Custom Tools</h3>
          <ul class="bento-list">
            <li style="--li-i:0">AI assistants built for your business</li>
            <li style="--li-i:1">Workflows automated end-to-end</li>
            <li style="--li-i:2">Tools tailored to your operations</li>
            <li style="--li-i:3">Systems that grow with your team</li>
          </ul>
          <p class="svc-footer">Built to work with you.</p>
        </div>
      </article>

    </div>
  </section>

  <!-- ══════════════════════════════ BOLD DARK STATEMENT -->
  <section class="bold-statement" aria-label="VA promise">
    <div class="bs-inner">
      <p class="bs-line" data-reveal="up" data-reveal-delay="0">Your time is finite.</p>
      <p class="bs-line bs-accent" data-reveal="up" data-reveal-delay="140">Delegate everything else.</p>
    </div>
    <div class="bs-reel" aria-hidden="true">
      <div class="bs-reel-track">
        <span class="bsr-word">Inbox</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Calendar</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Projects</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Follow-ups</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Reports</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Operations</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Inbox</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Calendar</span><span class="bsr-sep">—</span>
        <span class="bsr-word">Projects</span><span class="bsr-sep">—</span>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════ LIVE DESK FEED -->
  <div class="desk-feed" aria-hidden="true">
    <span class="df-badge"><span class="df-dot"></span>Live</span>
    <div class="df-track-wrap">
      <div class="df-track">
        {[
          "Inbox cleared — 47 emails handled this morning",
          "3 executive calls blocked and confirmed",
          "Weekly report prepared and sent to stakeholders",
          "Project timeline updated — milestones on track",
          "Follow-up sequence sent to 5 warm leads",
          "Deep work calendar blocks set — zero conflicts",
          "Invoice batch reviewed and filed",
          "Inbox cleared — 47 emails handled this morning",
          "3 executive calls blocked and confirmed",
          "Weekly report prepared and sent to stakeholders",
        ].map((item) => (
          <>
            <span class="df-item">{item}</span>
            <span class="df-sep" aria-hidden="true">·</span>
          </>
        ))}
      </div>
    </div>
  </div>

  <!-- ═══════════════════════════════════════════════════════ CTA -->
  <section class="va-cta-section" aria-label="Hire a virtual assistant" data-reveal="fade" data-bee-final>
    <h2 class="cta-title" data-blur-in>Hire your virtual assistant today.</h2>
    <p class="cta-sub" data-blur-in data-blur-delay="150">Side by side. So you don't carry it alone.</p>
    <div data-stagger data-stagger-gap="80" class="cta-links">
      <a class="pill pill--filled magnetic" href="https://wa.me/27602551513">WhatsApp us →</a>
      <a class="pill pill--outline magnetic" href="mailto:info@lulidigital.co.za">Email the desk →</a>
    </div>
  </section>

</BaseLayout>


<style>
  /* ── Hero ───────────────────────────────────────────────────── */
  .va-hero {
    --va-blue: #284c63;
    --va-blue-soft: rgba(40,76,99,.12);
    --va-paper: #f8f6f0;
    position: relative;
    overflow: hidden;
    padding: 88px 0 72px;
    isolation: isolate;
    background:
      linear-gradient(115deg, rgba(40,76,99,.12), transparent 42%),
      linear-gradient(180deg, rgba(255,255,255,.34), transparent 76%);
  }
  .va-desk-field {
    position: absolute;
    inset: 0;
    z-index: -1;
    pointer-events: none;
    background:
      linear-gradient(90deg, rgba(40,76,99,.08) 1px, transparent 1px) 0 0 / 64px 64px,
      linear-gradient(0deg, rgba(40,76,99,.06) 1px, transparent 1px) 0 0 / 64px 64px,
      radial-gradient(circle at 18% 24%, rgba(40,76,99,.18), transparent 28%);
    mask-image: linear-gradient(90deg, transparent, #000 12%, #000 88%, transparent);
  }
  .va-desk-field::after {
    content: "";
    position: absolute;
    left: 8%;
    right: 8%;
    bottom: 14%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(40,76,99,.32), transparent);
    animation: deskSweep 4.8s ease-in-out infinite;
  }
  @keyframes deskSweep {
    0%,100% { transform: translateY(0); opacity: .25; }
    50% { transform: translateY(-26px); opacity: .7; }
  }
  .va-eyebrow { color: var(--va-blue); }

  .va-hero-split {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(320px, 420px);
    gap: clamp(28px, 5vw, 48px);
    align-items: center;
  }
  .va-hero-text { text-align: left; }

  .va-title {
    font-size: clamp(34px, 4.8vw, 62px);
    font-weight: 700;
    line-height: 1.06;
    letter-spacing: -.035em;
    margin: 14px 0 20px;
    font-family: var(--font-display, "Fraunces", ui-serif, Georgia, serif);
    color: #172229;
    max-width: 600px;
  }
  .va-sub {
    font-size: clamp(15px, 1.8vw, 17px);
    line-height: 1.65;
    color: var(--muted, rgba(20,19,18,.66));
    margin: 0 0 32px;
    max-width: 480px;
  }
  .va-ctas { display: flex; gap: 12px; flex-wrap: wrap; }

  /* Pills */
  .pill {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 13px 26px; border-radius: 999px;
    font-family: var(--font-body, "DM Sans", sans-serif);
    font-size: 12px; letter-spacing: .1em; text-transform: uppercase;
    font-weight: 700; text-decoration: none; cursor: pointer;
    will-change: transform;
    transition: box-shadow 250ms ease, background 200ms ease, color 200ms ease;
  }
  .pill--filled { background: var(--brand); color: #f9f5ef; box-shadow: 0 8px 28px rgba(123,45,45,.35); }
  .pill--filled:hover { box-shadow: 0 14px 36px rgba(123,45,45,.48); background: #6a2828; }
  .va-hero .pill--filled { background: var(--va-blue); box-shadow: 0 8px 24px rgba(40,76,99,.26); }
  .va-hero .pill--filled:hover { background: #1f3d50; box-shadow: 0 14px 32px rgba(40,76,99,.34); }
  .pill--outline { border: 1.5px solid var(--brand); color: var(--brand); }
  .va-hero .pill--outline { border-color: var(--va-blue); color: var(--va-blue); }
  .pill--outline:hover { background: rgba(123,45,45,.07); }
  .va-hero .pill--outline:hover { background: rgba(40,76,99,.08); }

  /* ── Task Board Character ───────────────────────────────────── */
  .va-board {
    position: relative;
    background: var(--va-paper);
    border: 1px solid rgba(40,76,99,.16);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 18px 38px rgba(23,34,41,.12), 0 1px 0 rgba(255,255,255,.7) inset;
    animation: boardFloat 5s ease-in-out infinite;
    will-change: transform;
  }
  .va-board::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(40,76,99,.06), transparent);
    transform: translateX(-100%);
    animation: taskReview 5.5s ease-in-out infinite;
    pointer-events: none;
  }
  @keyframes taskReview {
    0%, 35% { transform: translateX(-100%); }
    70%, 100% { transform: translateX(100%); }
  }
  @keyframes boardFloat {
    0%,100% { transform: translateY(0) rotate(0deg); }
    40%     { transform: translateY(-10px) rotate(-.4deg); }
    70%     { transform: translateY(-5px) rotate(.2deg); }
  }
  .vb-header {
    display: flex; align-items: center; gap: 7px;
    padding: 14px 18px 10px;
    border-bottom: 1px solid rgba(30,24,22,.08);
    background: rgba(40,76,99,.06);
  }
  .vb-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }
  .vb-dot--r { background: #ff5f57; }
  .vb-dot--y { background: #febc2e; }
  .vb-dot--g { background: #28c840; }
  .vb-title-bar {
    font-size: 11px; letter-spacing: .12em; text-transform: uppercase;
    color: rgba(23,34,41,.52); font-weight: 700;
    font-family: var(--font-body, "DM Sans", sans-serif);
    margin-left: 6px;
  }
  .vb-tasks { padding: 14px 18px; display: flex; flex-direction: column; gap: 10px; }
  .vb-task {
    display: flex; align-items: center; gap: 10px;
    font-size: 13px; font-family: var(--font-body, "DM Sans", sans-serif);
    padding: 10px 12px; border-radius: 8px;
    opacity: 0;
    transform: translateX(-10px);
    animation: taskSlide 400ms cubic-bezier(.34,1.4,.64,1) forwards;
    animation-delay: calc(var(--ti, 0) * 180ms + 400ms);
  }
  @keyframes taskSlide {
    to { opacity: 1; transform: translateX(0); }
  }
  .vb-task--done {
    background: rgba(47,133,90,.09);
    color: rgba(20,19,18,.75);
  }
  .vb-task--active {
    background: rgba(40,76,99,.1);
    color: #172229;
  }
  .vb-task--pending {
    background: rgba(30,24,22,.04);
    color: var(--muted, rgba(20,19,18,.55));
  }
  .vb-check {
    font-size: 11px; color: #28c840; font-weight: 700; flex-shrink: 0;
  }
  .vb-spinner {
    width: 12px; height: 12px; border-radius: 50%; flex-shrink: 0;
    border: 2px solid rgba(40,76,99,.2); border-top-color: var(--va-blue);
    animation: spin 800ms linear infinite;
  }
  @keyframes spin { to { transform: rotate(360deg); } }
  .vb-ring {
    width: 12px; height: 12px; border-radius: 50%; flex-shrink: 0;
    border: 2px solid rgba(30,24,22,.18);
  }
  .vb-footer {
    display: flex; align-items: center; gap: 8px;
    padding: 10px 18px 14px;
    border-top: 1px solid rgba(30,24,22,.06);
  }
  .vb-status-dot {
    width: 7px; height: 7px; border-radius: 50%; background: #28c840;
    box-shadow: 0 0 8px rgba(40,200,64,.7); animation: badgePulse 2s ease-in-out infinite;
  }
  .vb-status-text {
    font-size: 11px; letter-spacing: .1em; text-transform: uppercase;
    color: rgba(23,34,41,.52); font-family: var(--font-body, "DM Sans", sans-serif);
    font-weight: 600;
  }

  /* ── Delegation ─────────────────────────────────────────────── */
  .delegation-section { padding: 72px 0 80px; border-top: 1px solid rgba(30,24,22,.1); }
  .delegation-wrap { margin-top: 28px; }
  .delegation-wrap {
    border-radius: 12px;
    padding: 24px;
    background: linear-gradient(135deg, rgba(40,76,99,.08), rgba(255,255,255,.24));
    border: 1px solid rgba(40,76,99,.12);
  }
  .delegation-svg { width: 100%; max-width: 680px; height: auto; display: block; margin: 0 auto; }
  .delegation-svg .del-big { font-family: ui-serif, Georgia, serif; font-size: 15px; font-weight: 700; fill: rgba(20,19,18,.9); }
  .delegation-svg .del-big-inv { font-family: ui-serif, Georgia, serif; font-size: 14px; font-weight: 700; fill: rgba(243,239,231,.95); }
  .delegation-svg .del-small { font-family: ui-sans-serif, system-ui, sans-serif; font-size: 10px; fill: rgba(20,19,18,.5); letter-spacing: .04em; }
  .delegation-svg .del-small-inv { font-family: ui-sans-serif, system-ui, sans-serif; font-size: 10px; fill: rgba(243,239,231,.7); letter-spacing: .04em; }
  .delegation-ascii { display: none; }

  /* ── Services ───────────────────────────────────────────────── */
  .va-services { padding: 72px 0 80px; border-top: 1px solid rgba(30,24,22,.1); }
  .svc-card { padding: 28px 24px; min-height: 260px; }
  .va-services .svc-card {
    border-radius: 12px;
    border-color: rgba(40,76,99,.16);
    background:
      linear-gradient(180deg, rgba(255,255,255,.54), rgba(248,246,240,.84)),
      var(--paper-card);
  }
  .va-services .svc-card:hover {
    background: linear-gradient(160deg, #16262f, #284c63 68%, #376a7f);
  }
  .svc-icon {
    width: 40px; height: 40px; border-radius: 9px; background: var(--va-blue-soft);
    display: grid; place-items: center; color: var(--va-blue); flex-shrink: 0;
    transition: background 350ms ease, color 300ms ease, transform 400ms cubic-bezier(.34,1.56,.64,1);
  }
  .svc-card:hover .svc-icon { background: rgba(40,76,99,.22); color: #f9f5ef; transform: rotate(-4deg) scale(1.08); }
  .svc-title {
    font-size: clamp(17px, 1.9vw, 21px); font-weight: 700; margin: 0;
    letter-spacing: -.015em;
    font-family: var(--font-display, "Fraunces", ui-serif, Georgia, serif);
    line-height: 1.2; color: var(--ink, #141312);
    transition: color 300ms ease;
  }
  .svc-card:hover .svc-title { color: rgba(245,240,232,.95); }
  .bento-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 7px; flex: 1; }
  .bento-list li {
    font-size: 13.5px; line-height: 1.55; color: var(--muted, rgba(20,19,18,.66));
    padding-left: 14px; position: relative;
    font-family: var(--font-body, "DM Sans", sans-serif);
    transform: translateX(-8px);
    transition: transform 420ms cubic-bezier(.16,1,.3,1), color 300ms ease;
    transition-delay: calc(var(--li-i, 0) * 55ms + 60ms);
  }
  .bento-list li::before { content: "–"; position: absolute; left: 0; color: var(--va-blue); opacity: .65; transition: color 300ms ease; }
  .svc-card:hover .bento-list li { transform: translateX(0); color: rgba(245,240,232,.7); }
  .svc-card:hover .bento-list li::before { color: rgba(245,240,232,.8); opacity: .9; }
  .svc-footer {
    font-size: 11px; letter-spacing: .12em; text-transform: uppercase;
    color: var(--va-blue); font-weight: 700; margin: 0; margin-top: auto;
    font-family: var(--font-body, "DM Sans", sans-serif);
    transition: color 300ms ease;
  }
  .svc-card:hover .svc-footer { color: rgba(245,240,232,.45); }

  /* ── CTA ────────────────────────────────────────────────────── */
  .va-cta-section { text-align: center; padding: 80px 0 40px; border-top: 1px solid rgba(30,24,22,.1); }
  .cta-title {
    font-size: clamp(28px, 4vw, 48px); font-weight: 700; letter-spacing: -.03em; margin: 0 0 10px;
    font-family: var(--font-display, "Fraunces", ui-serif, Georgia, serif);
  }
  .cta-sub { font-size: 15px; color: var(--muted, rgba(20,19,18,.62)); margin: 0 0 28px; font-family: var(--font-body, "DM Sans", sans-serif); }
  .cta-links { display: flex; justify-content: center; gap: 12px; flex-wrap: wrap; }

  /* ── Responsive ─────────────────────────────────────────────── */
  @media (max-width: 900px) {
    .va-hero-split { grid-template-columns: 1fr; }
    .va-hero-text { text-align: center; }
    .va-title { max-width: 100%; margin-left: auto; margin-right: auto; }
    .va-sub { max-width: 100%; }
    .va-ctas { justify-content: center; }
    .va-board { max-width: 420px; margin: 0 auto; }
  }
  @media (max-width: 520px) {
    .delegation-svg { display: none; }
    .delegation-ascii { display: block; font-size: 10px; padding: 18px 14px; }
    .delegation-wrap { padding: 14px; }
    .va-hero { padding: 48px 0 42px; }
    .va-title { font-size: clamp(32px, 11vw, 44px); }
    .va-sub { font-size: 15px; line-height: 1.58; }
    .va-ctas .pill { width: 100%; justify-content: center; }
    .va-board { border-radius: 12px; animation-duration: 8s; }
    .vb-header { padding: 12px 14px 9px; }
    .vb-tasks { padding: 12px; gap: 8px; }
    .vb-task { font-size: 12.5px; align-items: flex-start; }
  }
  @media (max-width: 600px) {
    .bento-grid { grid-template-columns: 1fr; }
  }
  @media (prefers-reduced-motion: reduce) {
    .va-desk-field::after,
    .va-board,
    .va-board::before,
    .vb-task,
    .vb-spinner,
    .vb-status-dot { animation: none !important; }
    .vb-task { opacity: 1; transform: none; }
  }
</style>
